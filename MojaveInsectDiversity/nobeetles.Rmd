---
title: "Larrea Insect Diversity - no beetles"
author: "Jenna Braun"
date: "April 3, 2018"
output: html_document
---

```
library(dplyr)
library(tidyr)
library(ggplot2)
library(vegan)
library(lme4)
library(fitdistrplus)
```


```{r cars}
metadata <- read.csv("Data/metadata_nobeetle.csv")
insects <- read.csv("Data/wide_nobeetle.csv")
str(metadata)
str(insects)
#summary(metadata)
#summary(insects)
```

## What is the distribution for abundances?

```{r}
ggplot(metadata, aes(abun)) + geom_density()
```

```{r}
abun.normal <- fitdist(metadata$abun, "norm", method = c("mle", "mme", "qme", "mge"))
abun.poisson <- fitdist(metadata$abun, "pois", method = c("mle", "mme", "qme", "mge"))
abun.gamma <- fitdist(metadata$abun, "gamma", method = "mge")

shapiro.test(metadata$abun)
```

